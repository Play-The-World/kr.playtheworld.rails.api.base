version: '3'

services:
  app:
    build: 
      context: ../../
      dockerfile: ./.configs/production/Dockerfile
    restart: always
    depends_on: 
      - db
    entrypoint: ./.configs/production/start-up.sh
    environment:
      DB_USERNAME: root
      DB_PASSWORD: Rhemspdla1!
      DB_DATABASE: new_playthe.world
      DB_PORT: 3306
      DB_HOST: nas.playthe.world
      RACK_ENV: production
      RAILS_ENV: production
      RAILS_MAX_THREADS: 5
    volumes:
      - ../../.docker/log/app:/app/log
      - ../../.docker/storage:/app/storage
  
  nginx:
    image: nginx:alpine
    restart: always
    depends_on: 
      - app
    ports:
      - 80:80
      - 443:443
    volumes: 
      - ../../.docker/log/nginx:/var/log/nginx
      - ./nginx/conf.d:/etc/nginx/conf.d:ro